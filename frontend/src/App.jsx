/**
 * Main App Component
 * 
 * This is the root component that renders the main layout.
 * It imports and uses the Layout component which contains
 * the header, main content area, and footer.
 */

import "./App.css";
import Layout from "./layout/Layout";

function App() {
  return (
    <Layout/>
  );
}

export default App;
